[user]
name = kh3phr3n
email = kh3phr3n@nixsys.fr
signingkey = F185DC316E659F24

[core]
editor = nvim
pager = diff-so-fancy | less

[interactive]
diffFilter = diff-so-fancy --patch

[merge]
tool = nvim -d

[push]
default = matching

[color]
grep = auto
diff = auto
status = auto
branch = auto
interactive = auto

[alias]
df = diff
pl = pull
sh = show
ss = stash
bl = blame
cl = clone
me = merge
br = branch
ci = commit
re = restore
co = checkout
po = push origin
pb = push backup
ds = diff --stat
rs = reset --soft
rh = reset --hard
st = status -s -b
lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'

